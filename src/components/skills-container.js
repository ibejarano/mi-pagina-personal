import React from 'react'

const SkillsContainer = (props) => {
  // paso props con tipo de items (backend o frontend)
  // texto con lo que debe decir
  return <div>Hello world! </div>

}
export default SkillsContainer
